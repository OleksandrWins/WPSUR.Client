import { ListFormat } from "typescript";

interface Post { 
  id: string;
  title: string;
  body: string;
  mainTag: string;
  subTags: ListFormat;
  createdBy: string;
  updatedDate?: Date;
  createdDate: Date
}

export default Post;